'use strict';

/**
 * event-speaker-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::event-speaker-page.event-speaker-page');